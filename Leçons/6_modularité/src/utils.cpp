#include <iostream>               // On utilise <nom> pour les inclusion native (fichier source)
#include "../include/utils.hpp"   // On utilise "nom + extention" pour les inclusion locale

using namespace std;

void helloWorld()
{
    cout << "Hello World!" << endl;
}